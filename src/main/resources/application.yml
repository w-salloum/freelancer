spring:
    application:
      name: freelancer
    datasource:
        url: jdbc:mysql://localhost:3306/freelancer-db
        username: user1
        password: password1
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
          ddl-auto: update
        show-sql: true
        properties:
          hibernate:
            dialect: org.hibernate.dialect.MySQL8Dialectk